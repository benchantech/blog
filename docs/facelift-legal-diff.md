# Facelift — legal diff

The Phase 11 written diff lives at [docs/facelift-copy-diff.md](./facelift-copy-diff.md), which is the filename plan §8b.4 and the Phase 11 task table both name.

This file exists only so that either name finds it. It carries no content of its own — a second copy of the diff would be the drift the diff is about.
