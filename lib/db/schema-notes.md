# Neon-Ready Schema Notes

The initial Next.js version renders from typed local data. Keep the database
boundary server-only when Neon is added.

Future tables can mirror the local model:

- `destinations`
- `stakeholder_routes`
- `route_nodes`
- `route_options`
- `library_items`
- `transcripts`
- `transcript_segments`
- `retrieval_events`
- `route_events`
