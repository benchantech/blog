import type { Metadata } from "next";
import Link from "next/link";
import { OPERATING_METRICS_COPY, SHIPPED_OUTCOMES, USEFUL_SHIPPED_OUTCOME_DEFINITION } from "@/content/company/outcomes";
import styles from "@/components/company/experiment.module.css";
export const metadata:Metadata={title:OPERATING_METRICS_COPY.metadataTitle,description:OPERATING_METRICS_COPY.metadataDescription,alternates:{canonical:"/experiments/operating-metrics"}};
export default function Page(){return <article className={styles.page}><header className={styles.hero}><div className={styles.inner}><p className={styles.eyebrow}>{OPERATING_METRICS_COPY.eyebrow}</p><h1 className={styles.title}>{OPERATING_METRICS_COPY.title}</h1><p className={styles.question}>{USEFUL_SHIPPED_OUTCOME_DEFINITION}</p></div></header><section className={styles.section}><div className={styles.inner}><p className={styles.eyebrow}>{OPERATING_METRICS_COPY.logLabel}</p><ul className={styles.list}>{SHIPPED_OUTCOMES.map((item)=><li key={item.id}><strong>{item.outcome}</strong><br/>{OPERATING_METRICS_COPY.destinationLabel}: {item.durableDestination}<br/>{OPERATING_METRICS_COPY.captainMinutesLabel}: {item.captainMinutes ?? OPERATING_METRICS_COPY.unknownLabel}{item.captainMinutesNote ? ` — ${item.captainMinutesNote}` : ""}</li>)}</ul><p className={styles.body}>{OPERATING_METRICS_COPY.interventionNote}</p><Link className={styles.back} href="/experiments">{OPERATING_METRICS_COPY.backLabel}</Link></div></section></article>}
