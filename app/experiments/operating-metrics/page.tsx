import type { Metadata } from "next";
import Link from "next/link";
import { SHIPPED_OUTCOMES, USEFUL_SHIPPED_OUTCOME_DEFINITION } from "@/content/company/outcomes";
import styles from "@/components/company/experiment.module.css";
export const metadata:Metadata={title:"Operating Metrics | BenChanTech",description:"How Ben Chan Tech measures Captain effort against useful AI-assisted shipped outcomes.",alternates:{canonical:"/experiments/operating-metrics"}};
export default function Page(){return <article className={styles.page}><header className={styles.hero}><div className={styles.inner}><p className={styles.eyebrow}>$20 company · measurement</p><h1 className={styles.title}>Operating metrics</h1><p className={styles.question}>{USEFUL_SHIPPED_OUTCOME_DEFINITION}</p></div></header><section className={styles.section}><div className={styles.inner}><p className={styles.eyebrow}>Current outcome log</p><ul className={styles.list}>{SHIPPED_OUTCOMES.map((item)=><li key={item.id}><strong>{item.outcome}</strong><br/>Destination: {item.durableDestination}<br/>Captain minutes: {item.captainMinutes ?? "unknown"}{item.captainMinutesNote ? ` — ${item.captainMinutesNote}` : ""}</li>)}</ul><p className={styles.body}>Interventions are classified as judgment, authority, missing context, execution failure, or tooling. Unknown historical values remain unknown rather than being reconstructed from memory.</p><Link className={styles.back} href="/experiments">All experiments</Link></div></section></article>}
