export default function NeonPage() {
  return (
    <section className="detail-page">
      <p className="eyebrow">Neon route</p>
      <h1>Postgres is the durable substrate for expert-learning infrastructure.</h1>
      <p>
        This page will carry the Neon narrative: deterministic routing before AI calls, transcript-grounded retrieval,
        route events, and a low-cost database-backed wedge for the BenChanViolin Library.
      </p>
      <div className="detail-grid">
        <article>
          <h2>Local first</h2>
          <p>The first production version renders from typed local data, so the route graph remains testable.</p>
        </article>
        <article>
          <h2>Database ready</h2>
          <p>A server-only data boundary is present for future Neon tables without changing client components.</p>
        </article>
      </div>
    </section>
  );
}
